server {
    listen 80;

    location / {
        root {{ static_path }};
    }

    location /applications/ {
        proxy_pass http://server:{{ server_port }}/applications/;
    }

    location ~ "/^[a-zA-Z0-9]{8}$" {
        proxy_pass http://unit:80;
    }
}
